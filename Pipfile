[[source]]
url = "https://pypi.org/simple"
verify_ssl = true
name = "pypi"

[packages]
boto3 = "*"

[dev-packages]
pytest = "*"

[requires]
python_version = "3.11"

[scripts]
build = "pipenv install --deploy"
build-dev = "pipenv install"
